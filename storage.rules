rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Cada usuario autenticado solo puede leer/escribir sus propios documentos
    match /documentos/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
